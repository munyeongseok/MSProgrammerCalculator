<Window x:Class="MSProgrammerCalculator.Calculator"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSProgrammerCalculator"
        xmlns:views="clr-namespace:MSProgrammerCalculator.Views"
        mc:Ignorable="d"
        Title="계산기"
        Width="580"
        Height="510"
        MinWidth="340"
        MinHeight="510"
        SizeChanged="Window_SizeChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" MaxHeight="70"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*" MaxHeight="70"/>
            <RowDefinition Height="2*" MaxHeight="70"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition x:Name="MemoryPanelColumn" Width="8*" MaxWidth="320"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Background="Red"/>
        <Border Grid.Row="1" Background="Green"/>
        <Border Grid.Row="2" Background="Blue"/>
        <Border Grid.Row="3" Background="Yellow"/>
        <views:FullKeypadView x:Name="FullKeypadView" Grid.Row="4"/>
        <views:BitKeypadView x:Name="BitKeypadView" Grid.Row="4"/>
    </Grid>
</Window>
